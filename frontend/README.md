# Frontend (React + Vite)

## Run locally

1. Install dependencies:
   - `npm install`
2. Copy `.env.example` to `.env` if needed.
3. Start dev server:
   - `npm run dev`

Default URL: `http://localhost:5173`
